# Upstash Redis (via Vercel Marketplace) — required for Phase 6 forecast bridge
# These are auto-injected by Vercel when you connect an Upstash integration
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Secret for the /api/revalidate webhook (generate with: openssl rand -hex 32)
REVALIDATE_SECRET=
